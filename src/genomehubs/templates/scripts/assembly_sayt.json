{
  "script": {
    "lang": "mustache",
    "source": {
      "size": "{{size}}{{^size}}10{{/size}}",
      "query": {
        "bool": {
          "should": [
            {
              "prefix": {
                "assembly_id": {
                  "value": "{{searchTerm}}",
                  "boost": 3.0
                }
              }
            }
          ]
        }
      },
      "_source": ["assembly_id", "taxon_id", "scientific_name", "identifiers.*"]
    }
  }
}
