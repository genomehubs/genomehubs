{
  "script": {
    "lang": "mustache",
    "source": {
      "query": {
        "match": { "taxon_id": "{{taxon_id}}" }
      },
      "_source": ["taxon_id", "scientific_name", "parent", "attributes.*"]
    }
  }
}
